version: '3'
volumes:
  zinven_store:
networks:
  zinven_net:
    driver: bridge
services:
  db:
    image: postgres
    restart: always
    ports:
      - '5432:5432'
    networks:
      - zinven_net
    volumes:
      - zinven_store:/var/lib/postgresql/data
    environment:
      - DATABASE_HOST=127.0.0.1
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=zinven_db
      - POSTGRES_PORT=5432
  api:
    build: api/
    ports:
      - '8081:8081'
    networks:
      - zinven_net
    depends_on:
      - 'db'
    volumes:
      - type: bind
        source: ./api
        target: /app
    environment:
      - DATABASE_HOST=db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=zinven_db
      - POSTGRES_PORT=5432
  ui:
    build: ui/
    ports:
    - '3000:3000'
    networks:
      - zinven_net
    depends_on:
      - 'db'
    volumes:
      - type: bind
        source: ./ui/src
        target: /app/src